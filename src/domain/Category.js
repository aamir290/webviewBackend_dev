/**
 * Represent a category for chatbots.
 */
class Category {

  /**
   * Create new category
   * @param name category's name
   */
  constructor(id, name){
    this.id = id;
    this.name = name;
  }
}

module.exports = Category;
