/**
 * Server Class.
 * Setup server and launch.
 */

module.exports = class Server {

  /**
   * Init application.
   */
  constructor() {
  }

  async start() {
    return new Promise((resolve) => {
      resolve();
    });
  }
};
