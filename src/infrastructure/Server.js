/**
 * Server Class.
 * Setup server and launch.
 */

class Server {

  /**
   * Init application.
   */
  constructor() {
  }

  async start() {
    return new Promise((resolve) => {
      resolve();
    });
  }
}

module.exports = Server;
