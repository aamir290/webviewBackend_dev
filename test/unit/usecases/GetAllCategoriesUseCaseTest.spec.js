/**
 * Unit tests for GetAllCategoriesUseCase
 */
const { expect } = require('chai');

describe('GetAllCategoriesUseCase', () => {

  context('when usecase is called', () => {
    it('works', ()=>{
      expect('ok').to.be.equal('ok');
    });
  });
});
